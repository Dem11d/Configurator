    jQuery(function ($) {        $("a.topopup").click(function () {            loading(); // loading            setTimeout(function () { // then show popup, deley in .5 second                loadPopup(); // function show popup                $('html, body').animate({ scrollTop: 0 }, 'slow');                var uId = uniqueid();                $('#txtQty').val(1);                $("#design_code").val(uId);                setStartImage("/img/customImage/android/HTC-one_M9.png"); //design type            }, 500); // .5 second            return false;        });        /* event for close the popup */        $("div.close").hover(            function () {                $('span.ecs_tooltip').show();            },            function () {                $('span.ecs_tooltip').hide();            }        );        $("div.close").click(function () {            var iAnswer = confirm("If you close this popup then you can loose your design so are you sure want to close popup ?");            if (iAnswer) {                cleanMyStoreArray();                disablePopup();  // function close pop up            }        });        $(this).keyup(function (event) {            if (event.which == 27) { // 27 is 'Ecs' in the keyboard                disablePopup();  // function close pop up            }        });        $("div#backgroundPopup").click(function () {            disablePopup();  // function close pop up        });        /************** start: functions. **************/        function loading() {            $("div.loader").show();        }        function closeloading() {            $("div.loader").fadeOut('normal');        }        var popupStatus = 0; // set value        function loadPopup() {            if (popupStatus == 0) { // if value is 0, show popup                closeloading(); // fadeout loading                $("#toPopup").fadeIn(0500); // fadein popup div                $("#backgroundPopup").css("opacity", "0.7"); // css opacity, supports IE7, IE8                $("#backgroundPopup").fadeIn(0001);                popupStatus = 1; // and set value to 1            }        }        function disablePopup() {            if (popupStatus == 1) { // if value is 1, close popup                $("#toPopup").fadeOut("normal");                $("#backgroundPopup").fadeOut("normal");                popupStatus = 0;  // and set value to 0            }        }        $('#front').show();        /************** end: functions. **************/        /*Preview Popup*/        $("a.preview_popup").click(function () {            loading_preview(); // loading            setTimeout(function () { // then show popup, deley in .5 second                loadPopupPreview(); // function show popup            }, 100); // .5 second            return false;        });        /* event for close the popup */        $("div.close_preview").hover(            function () {                $('span.ecs_tooltip').show();            },            function () {                $('span.ecs_tooltip').hide();            }        );        $("div.close_preview").click(function () {            disablePopupPreview();        });        $(this).keyup(function (event) {            if (event.which == 27) { // 27 is 'Ecs' in the keyboard                disablePopupPreview();  // function close pop up            }        });        $("div.backgroundPopup_preview").click(function () {            disablePopupPreview();  // function close pop up        });        /************** start: functions. **************/        function loading_preview() {            $("div.loader_preview").show();        }        function closeloadingPreview() {            $("div.loader_preview").fadeOut('normal');        }        var popupStatusPreview = 0; // set value        function loadPopupPreview() {            if (popupStatusPreview == 0) { // if value is 0, show popup                closeloadingPreview(); // fadeout loading                $(".toPopup_preview").css("display", "block");                $(".toPopup_preview").css("z-index", "9999");                $(".toPopup_preview").fadeIn(500); // fadein popup div                $(".backgroundPopup_preview").css("display", "block"); // css opacity, supports IE7, IE8                $(".backgroundPopup_preview").fadeIn(0001);                popupStatusPreview = 1; // and set value to 1            }        }        function disablePopupPreview() {            if (popupStatusPreview == 1) { // if value is 1, close popup                $(".toPopup_preview").fadeOut("normal");                $(".backgroundPopup_preview").fadeOut("normal");                popupStatusPreview = 0;  // and set value to 0            }        }    }); // jQuery End    function uniqueid() {        var idstr = String.fromCharCode(Math.floor((Math.random() * 25) + 65));        do {            var ascicode = Math.floor((Math.random() * 42) + 48);            if (ascicode < 58 || ascicode > 64) {                idstr += String.fromCharCode(ascicode);            }        } while (idstr.length < 32);        return (idstr);    }    function showtab(obj) {        $("#tab_0").css({'background': '#666766', 'color': '#ababab', 'border-radius': '0'});        $("#tab_1").css({'background': '#666766', 'color': '#ababab', 'border-radius': '0'});        $("#tab_2").css({'background': '#666766', 'color': '#ababab', 'border-radius': '0'});        $("#tab_3").css({'background': '#666766', 'color': '#ababab', 'border-radius': '0'});        $("#tab_4").css({'background': '#666766', 'color': '#ababab', 'border-radius': '0'});        $("#tab_5").css({'background': '#666766', 'color': '#ababab', 'border-radius': '0'});        $(".tab_content").hide();        $("#content_" + obj).show();        $("#tab_" + obj).css({'background': '#81D8D0', 'color': '#0a0a0a', 'border-radius': '0px 0px 0 0'});    }